const numbers = [10, 20, 30];
let sum = 0;

for (const number of numbers) {
    sum += number;
}

console.log(sum);

const fullName = "Matheus Duarte";

for (const letter of fullName) {
    console.log(letter);
}
